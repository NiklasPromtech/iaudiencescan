

## Fix domain in DashboardFrame browser chrome

**File:** `src/components/landing/DashboardFrame.tsx`

**Change:** On line 16, replace `app.audiencescan.xyz/overview` with `audiencescan.io/overview`.

One-line change, no other files affected.

