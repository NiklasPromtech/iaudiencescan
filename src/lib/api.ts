// This file re-exports everything from the split modules for backward compatibility.
// New code should import directly from '@/lib/api' (which resolves to api/index.ts).
export * from "./api/index";
